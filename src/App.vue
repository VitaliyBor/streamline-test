<script setup>
import { createApp } from 'vue';
import HelloWorld from './components/HelloWorld.vue'
import TheWelcome from './components/TheWelcome.vue'
import TreeView from './components/TreeView.vue'
import SelectedList from './components/SelectedList.vue'

</script>

<template>

  <main>

    <div class="wrapper">
    <TreeView />
    <SelectedList/>
    </div>

  </main>
</template>

<script>
export default {
  name: 'app',
  components:{
  },
  mounted(){
    this.$store.dispatch('loadTree');
  },
  computed:{
    tree_data(){ return this.$store.state.tree_data},
  }
};
</script>

<style scoped>
header {
  line-height: 1.5;
}

.logo {
  display: block;
  margin: 0 auto 2rem;
}

@media (min-width: 1024px) {
  header {
    display: flex;
    place-items: center;
    padding-right: calc(var(--section-gap) / 2);
  }

  .logo {
    margin: 0 2rem 0 0;
  }

  header .wrapper {
    display: flex;
    place-items: flex-start;
    flex-wrap: wrap;
  }
}
</style>
